---
import "../styles/global.css";
import Heather from "../components/Cabecera.astro";
import Layout from "../layouts/Layout.astro";
import RegisterForm from "../components/forms/RegisterForm.astro";
const a = import.meta.env.JAVI_URL;
---

<!-- Pagina de resgistro --><!-- pagina para registrarse -->
<Layout testTitle="Registro">
        <Heather
                showCompare={false}
                showMediciones={false}
                showSensores={false}
                showPrediciones={false}
                showPh={false}
                showTemp={false}
                showBack={false}
        />
        <RegisterForm />
</Layout>
